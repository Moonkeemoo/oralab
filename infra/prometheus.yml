global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: ora2-otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
        labels:
          stack: ora2
